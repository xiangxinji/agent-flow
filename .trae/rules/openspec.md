# OpenSpec Integration Rule
You must ALWAYS adhere to the project specifications defined in the `.spec/` directory.

### Mandatory Context:
1. **Config:** Read `.spec/config.yml` to understand project scope and exclusion rules.

### Workflow:
- Before writing any code, you MUST generate a **Proposal** based on the current specs.
- Update `.spec/todo.md` after every task.