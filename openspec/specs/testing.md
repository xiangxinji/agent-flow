在进行测试任务时需要遵循一下要求：
1. 根据 json 的书写内容来判断有没有 node 或者 edge 被执行到来做为测试用例
2. 根据 json node 中的配置来进行多个维度的测试，例如当测试 iterator 节点时
    - 是否正确地迭代数组中的每个元素
    - 是否正确的执行了 next 节点
    - edge 有没有规划正确
3. 在执行测试任务时应该按照执行功能来进行拆分测试脚本,例如当测试 iterator 节点时
    - test-iterator.ts: 测试 iterator 节点的执行逻辑是否符合预期

